version: "3.9"

services:
  django:
    build:
      context: .
    volumes:
      - ./django_backend:/django_backend
    ports:
      - "8050:8050"
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8050"
